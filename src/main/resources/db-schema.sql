CREATE TABLE USERS(id INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY(START WITH 1,INCREMENT BY 1), lastname VARCHAR(256), firstname VARCHAR(256), age INTEGER not NULL, PRIMARY KEY(id));CREATE TABLE ORDERS (id INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY(START WITH 1,INCREMENT BY 1),firstName VARCHAR(256),secondName VARCHAR(256),PRIMARY KEY(id))